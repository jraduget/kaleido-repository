Welcome to KaleidoFoundry project !

Project description
-------------------

Kaleido Foundry is a java framework, based on jdk 6 and java ee 5 & 6 APIs. 
It is a simple and extensible technical base for managing : file store, cache, configuration, i18n labels & exception messages, naming service (jndi for ejb / datasource / jms queue...), mail sender service (async or not), asynchronous messaging (jms, tibco rdv / ems...). 

Moreover, it provides  a *context injection mechanism* using  *@Context* annotation for its components. You inject dynamically the component configuration, and the configuration management  is dynamics and centralized (across your cluster).

Kaleido Foundry has been designed to be *easily used and extended* by developer, and *easy to integrate* to your current java stack. 
So you can use open source or commercial libraries, your environment could be clustered or not, you could use a replicated or distributed caching, you could be hosted in the cloud like gae...

Requirements
------------
  * Java >= 1.6 (OpenJDK and Sun have been tested)

Modules
-------
  * kaleido-parent	KaleidoFoundry parent  
  * kaleido-core	KaleidoFoundry core module
	. ResourceStore api (file resource store api, http, ftp, classpath, webapp, jpa clob, ...
	. Caching api (local, ehcache, jboss cache, jboss infinispan, oracle coherence, gygaspace implementation) 
	. Configuration api (based on cache and resource store
	. I18n api for label & exceptions (resource bundle extention, based on cache and resource store)
	. Nnaming api (jndi lookup ejb, jms queue / topic, datasource, ...)
	. Context injection api (@Context enable runtime context injection, to core modules)
	. Other thinks like @NotNull, @NotYetImplemented, @PersistenceContext in unmanaged env...
  * kaleido-guice-integration	KaleidoFoundry guice integration module (for context injection)
  * kaledo-spring-integration	KaleidoFoundry spring integration module (for context injection)

Integration test modules 
------------------------
  * kaleido-it-parent
  * kaleido-it-core
  * kaleido-it-ejb
  * kaleido-it-war
  * kaleido-it-ear

Upcoming 
--------
  * kaleido-mail        KaleidoFoundry mailling module
  * kaleido-messaging   KaleidoFoundry messaging (asynchronous) module
  * kaleido-webadmin	KaleidoFoundry web admin (i18n, configuration, log, cache informations, ...)
  * kaleido-rules
  * kaleido-seurity
  * kaleido-report

